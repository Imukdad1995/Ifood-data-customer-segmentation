Ifood customer base clustering project by Ibrahim Ahmad

Ifood data prep.ipnyb is the raw data restrucuring, cleaning, exploration and preparing for clustering code.
Ifood data clustering.ipynb is the dimensionality reduction and clustering code.